import StubPage from '../../components/tft/StubPage';
export default function Page() {
  return <StubPage active="augments" title="Augments" hint="Augment-Tier-Liste pro Stage (Silver / Gold / Prismatic) — kommt in Stage 4." />;
}
