import StubPage from '../../components/tft/StubPage';
export default function Page() {
  return <StubPage active="units" title="Units" hint="Champion-Tier-Liste mit Item-Builds — kommt in Stage 4." />;
}
